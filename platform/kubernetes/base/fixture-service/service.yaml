apiVersion: v1
kind: Service
metadata:
  name: fixture-service
  labels:
    app.kubernetes.io/name: fixture-service
spec:
  selector:
    app.kubernetes.io/name: fixture-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
