apiVersion: apps/v1
kind: Deployment
metadata:
  name: results-service
  labels:
    app.kubernetes.io/name: results-service
    app.kubernetes.io/part-of: football-platform-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: results-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: results-service
    spec:
      serviceAccountName: results-service
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: results-service
          image: results-service:0.1.0
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 1000
            runAsNonRoot: true
            runAsUser: 1000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: results-service-database
                  key: url
            - name: FIXTURE_SERVICE_URL
              value: http://fixture-service
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /livez
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
