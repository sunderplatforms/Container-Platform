apiVersion: v1
kind: Service
metadata:
  name: results-service
  labels:
    app.kubernetes.io/name: results-service
spec:
  selector:
    app.kubernetes.io/name: results-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
