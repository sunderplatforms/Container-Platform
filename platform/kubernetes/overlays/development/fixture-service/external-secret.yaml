apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: fixture-service-database
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secrets-manager
  target:
    name: fixture-service-database
    creationPolicy: Owner
  data:
    - secretKey: url
      remoteRef:
        key: match-data/development/fixture-service
        property: databaseUrl
