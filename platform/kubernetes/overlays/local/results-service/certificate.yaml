apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: results-service-tls
  namespace: match-data
spec:
  secretName: results-service-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - results-service.match-data.svc.cluster.local
